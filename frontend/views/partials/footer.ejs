  <footer class="footer">
    <div class="container footer-content">
      <div class="footer-left">
        <span class="brand-mark sm">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
            <path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71" />
            <path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71" />
          </svg>
        </span>
        <strong>Shurly</strong> &middot; Rust ile yazılmış URL kısaltma servisi
      </div>
      <div class="footer-right">
        <span class="chip">Actix-Web</span>
        <span class="chip">Tokio</span>
        <span class="chip">sqlx</span>
        <span class="chip">PostgreSQL</span>
        <span class="chip">Redis</span>
        <span class="chip">Docker</span>
      </div>
    </div>
  </footer>

  <div id="toast" class="toast" aria-live="polite"></div>
  <script>
    // Header'daki tema toggle butonu (her sayfada aktif)
    (function () {
      var btn = document.getElementById('themeToggle');
      if (!btn) return;
      btn.addEventListener('click', function () {
        var current = document.documentElement.getAttribute('data-theme') || 'dark';
        var next = current === 'dark' ? 'light' : 'dark';
        document.documentElement.setAttribute('data-theme', next);
        try { localStorage.setItem('shurly-theme', next); } catch (e) {}
      });
    })();
  </script>
</body>
</html>
